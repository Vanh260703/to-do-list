<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Chỉnh Sửa Task</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet" />
  <style>
    body {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
    }
    .edit-task-card {
      background: #fff;
      border-radius: 16px;
      box-shadow: 0 10px 25px rgba(0,0,0,0.1);
      padding: 2rem;
      max-width: 600px;
      width: 100%;
    }
    .edit-task-card h2 {
      font-weight: 800;
      margin-bottom: 1.5rem;
      color: #333;
    }
    .form-label {
      font-weight: 600;
      margin-bottom: 0.5rem;
    }
    .form-control, .form-select {
      border-radius: 10px;
    }
    .btn-primary {
      border-radius: 10px;
      font-weight: 600;
    }
    .back-link {
      text-decoration: none;
      font-size: 0.9rem;
      display: inline-block;
      margin-bottom: 1rem;
      color: #6c757d;
    }
    .back-link i {
      margin-right: 4px;
    }
  </style>
</head>
<body>
  <div class="edit-task-card">
    <a href="/me/manager-task" class="back-link">
      <i class="fas fa-arrow-left"></i> Quay về Manager Task
    </a>
    <h2><i class="fas fa-edit me-2"></i>Chỉnh Sửa Task</h2>
    <form action="/me/tasks/update/{{task._id}}?_method=PUT" method="POST">
      <div class="mb-3">
        <label for="content" class="form-label">Nội dung công việc</label>
        <input type="text" id="content" name="content" class="form-control" value="{{task.content}}" required />
      </div>
      <div class="mb-3">
       <label for="deadline" class="form-label">Thời hạn công việc</label>
        <select name="taskType" id="deadline" class="form-select" required>
            <option value="inDay">Công việc trong ngày</option>
            <option value="inWeek">Công việc trong tuần</option>
            <option value="inMonth">Công việc trong tháng</option>
        </select>
      </div>
      <button type="submit" class="btn btn-primary w-100">
        <i class="fas fa-save me-1"></i> Lưu Thay Đổi
      </button>
    </form>
  </div>
</body>
</html>